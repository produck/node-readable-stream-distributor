# 等待的守卫 —— JS 里那种"不是迭代"的 while

> 记录一个在 JS 里罕见、在并发领域却是标准做法的构造：用 `while`
> 反复重判条件的"等"。本文只描述构造本身与判断依据，不改动设计
> 结论；本仓库的具体实例见文末。

## 现象

`SourceConsumptionAgent.ensure()` 里的这一段：

```js
while (target >= this.pulledChunkCount && !sourceReader.done) {
  if (this.pulling === null) {
    this.pulling = this.pull().finally(() => (this.pulling = null));
  }

  await this.pulling;
}
```

第一眼它像轮询：一个条件、一个循环、条件不成立就再来一遍。
但它不是——每一轮都必须**真的推进一次**，没有"查而不做"的空转。

## 它是什么

普通循环在**推进自己的索引**；这个循环在**等待别人推进状态**。

- 它不是"迭代到条件成立"，而是"**等到**条件成立"——wait-until。
- 循环体不是"下一步"，而是"**让条件更接近成立的那一次推进**"。
- 迭代次数不来自任何集合的长度，而来自**目标与落点之间的差额**。

一句话定位：这里的 `while` 承担的是**同步语义**（synchronization），
不是控制流（control flow）。JS 日常里的循环几乎都是后者，所以才
显得"越界"。

## "守卫"的直觉是对的

退出条件就是后置条件。用 Hoare 三元组写出来：

```text
{I} while (G) { S } {I ∧ ¬G}
```

`while` 把 `¬G` 变成后置条件——也就是"我要的那个东西已经在了"。
于是 `ensure()` **返回这件事本身就是一条被兑现的断言**：

```text
ensure(target) 返回 ⇒ 目标块已落地，或源已终结
```

这条断言不是被调用者假定的，而是被循环"挣"出来的。

## 为什么是 `while` 而不是 `if`

这是它真正的理由，也是并发文献里半个世纪的老规矩：

- **等到被唤醒，条件不保证已经成立。** 如果"等一次"就能保证条件
  成立，`if` 就够；一旦唤醒可能落空（信号被别人抢走、唤醒后条件
  又变假、假唤醒），就必须回头重判。
- C++ 标准把带谓词的 `wait(lock, pred)` **直接定义成**这行 `while`；
  Rust 的对应物叫 `Condvar::wait_while(guard, |g| !pred)`。
- 所以这不是"为了保险多写一层"，而是：当"等"不是一次性的时候，
  `while` 才是如实的形态；`if` 是在赌"等一次必然成立"。

## 为什么 promise 不够：边沿 vs 电平

`await` 等的是**边沿**（某件事发生过一次），条件等待要的是**电平**
（状态此刻满足某个谓词）。JS 原生只给了前者。

promise 的三条性质，正好是条件等待需要的反面：

| promise 给的           | 条件等待需要的         |
| ---------------------- | ---------------------- |
| 只结算一次             | 同一条件会被反复满足   |
| 结算后不可重臂         | 边沿要能复供后来者     |
| 多等待者共享同一次结算 | 每个等待者有各自的判据 |

于是 `pulling` 的活是"把一条边沿存起来供人反复搭手"，`finally` 回位
是"重新上膛"，而"这条边沿是否满足我这个 `target`"promise 无从知晓
——这就是那圈重判存在的全部理由：

```text
while (¬P) { await 下一条边沿 }
```

`await` 只能表达右半格，`P` 与重判必须自己写。

JS 在这里既占便宜也吃亏：

- **便宜**：C 的 `mutex + condvar + while` 里，"判空与登记必须原子"
  由锁保证；JS 无抢占，同步块天然原子，所以**不需要锁**。
- **亏**：也正因没有阻塞原语，"等"本身没有原生表达，只能自己造槽。

原生里没有更合适的表达，逐个都能否掉：

- `await p`：等一次结算——一次性、不可重臂、多等待者共享同一次。
- `for await`：等一串边沿——只是把 `while` 换个壳，判据重判仍在。
- `EventTarget` + `once`：等一个事件——同 promise；订阅前已满足即
  lost wakeup。
- `AbortSignal`：等一次中止——一次性。
- `Promise.withResolvers()`：只省样板，不改语义。
- `Atomics.waitAsync`：这才是真正的电平等待，但只在共享内存 / worker
  成立，且它的标准用法仍然是 `while (lock, pred)`。
- `queueMicrotask` / `setImmediate`：那是让出，不是等。

### 想真正去掉循环，只能换做法

不换原语，而是维护**等待者登记表**：每次落地后扫表，把满足的
`{ target, resolve }` 逐个 settle。每个等待者只被放行一次，不再重判。

| 项     | 循环 + 槽                | 等待者登记表           |
| ------ | ------------------------ | ---------------------- |
| 状态   | 一个 `pulling`           | 一张表                 |
| 唤醒方 | 等待者自己重判           | 落地侧扫表 settle      |
| 终局   | 判据带 `!done`，抛错上抛 | 终局都要扫表，否则悬挂 |
| 背压   | 一轮不推进就再等         | 不推进时不能误扫表     |

那套其实是**把 condvar 手写出来**（C 的 cv 内部就是这张表）。
`SourceConsumptionAgent.ensure()` 不取，是因为单飞已经保证"一次拉取
服务所有入座者"，表长几乎恒为 1–3，三行循环更便宜。**位置门取了**
（`AbstractTransferrer` 的 `I.PENDING_RELEASES` + `I.SETTLE()`）：那里的
判据是"该位已被接受"，改变它的点有四个（入队、dump 落地、`SET_DONE`、
`FAIL`），循环版得靠"每次水位变化都广播一次"兜住漏发（而水位那处
其实放行不了任何人），表版让每个改变点自己结算、判据只写一处。它
说明了一件事：**循环不是省事，是把簿记换成了重判。**

### 所以 promise 够不够

- **编排**（orchestration）——谁在谁之后。那是有向图，promise 完胜。
- **协调**（coordination）——多个等待者围绕共享状态上的一个谓词：
  需要"状态 + 唤醒 + 重判"三件套，promise 只给了第二件。

`ensure()` 恰好压在两种模型的接缝上，而且是被**源 API 的形状**逼上
去的：`reader.read()` 每次只交一块（边沿），`ChunkReader` 问的却是
"第 `index` 块在不在"（电平）。边沿攒成电平总得有人做，做它的就是
那圈循环。

## 各传统的叫法

| 语境              | 惯用名                                       |
| ----------------- | -------------------------------------------- |
| 并发 / 监视器     | guarded wait、predicate wait、re-check loop  |
| POSIX / Java      | `while (!pred) cond_wait()` 惯用法           |
| C++ / Rust        | `wait(lock, pred)`、`wait_while(...)`        |
| 契约 / Hoare 逻辑 | guard-driven loop；退出谓词即后置条件        |
| 异步 JS           | await-until、single-flight + coalescing wait |
| 本仓库用词        | 醒来重判（不是轮询）；等到就绪               |

## 与近亲划清界限

| 构造             | 为什么不是它                                       |
| ---------------- | -------------------------------------------------- |
| busy-wait / spin | 空转烧 CPU；这里每轮都阻塞在共享的在途拉取上       |
| retry / backoff  | 因**失败**而重来；这里失败直接上抛，只因未满足而等 |
| data loop        | 次数来自集合长度；这里次数来自目标与落点之差       |
| polling          | 查而不做；这里每轮必须推进                         |

## 本仓库实例

同一构造在本仓库出现两次，构成两级"单飞"：

| 位置                              | 槽          | 信号              |
| --------------------------------- | ----------- | ----------------- |
| `SourceReader.read()`             | `I.READING` | 在途读的 settle   |
| `SourceConsumptionAgent.ensure()` | `pulling`   | 在途拉取的 settle |

位置门（`AbstractTransferrer` 的 `I.PENDING_RELEASES` + `I.SETTLE()`）面对
的是同一类问题，但它取了登记表形态（见上文），不在本表内。

两者的分工：前者守"不重复读源"，后者守"不重复落地"。两个槽都必须
**能被同步判定**（空闲即 `null`），且必须用 `finally` 回位——否则一次
失败就会把门焊死。

判据用的是**代理自己经手的块数**，而且由代理**自持**：`pulledChunkCount`
（每交接成功一块 +1）。它只记"经手过"——"可读"是存储层的契约（交接
settle ⇒ 可读），不是这个计数自证。`ensure` 因此**不认识存储层**——
`chunkStash` 与 `transferrer` 用哪一种，完整留在 `pull()` 的路由里。

这里曾经还有第二个计数：源侧读过多少块。它的自增发生在**交接之前**，
所以比代理侧的水位**领先 1**；用它当判据，等待者会在"源侧已经 +1、
交接还没完成"的那一格提前返回，在前沿拿到空答案——`ensure` 的前身正是
栽在这里。该计数已移除：没有任何读者，而"源侧读了多少"在调试时看代理侧
的 `pulledChunkCount` 就够。留下的是它的教训，不是它的字段。

完整调用链：

```text
ForkedReadableStream.pull → ChunkReader.$I.ENSURE_THEN_READ()
  → agent.ensure(target)      ← 本文的 while
    → sourceReader.read()     ← 另一个同型槽
```

## 这个构造成立的前提（义务）

1. **每轮必须推进。** 拉取要么落地一块（`pulledChunkCount + 1`），
   要么置 done，要么抛错。一旦允许"静默不推进"的一轮，循环就退化成
   空转。
2. **循环体必须保持廉价。** 等待者数量会放大循环体的执行次数：
   3 条 fork 同时读前沿 = 循环体执行 3 次，而真实拉取只有 1 次。
   所以背压这类逻辑要放进 `pull()`（单飞内部，一群等待者只守候
   一次），不能写进循环体（会被等待者数量放大）。
3. **"等"必须是等。** 若 `pull()` 里出现"这一轮先不落地"的分支，
   它必须 `await` 一个结算点，而不是 `return`。

## 危险性：义务没有语法承载

这个构造让人不安，不是错觉。promise 写错的失败模式是"非响即静默"；
这段循环写错的失败模式**必然是静默**：要么挂死（见「实测」最后一行），
要么下游拿到一个空块（前沿的空答案）。而且症状离病因很远——消费者
卡住的地方，和少写一行的地方，通常不在一个文件。

来源一句话就能说清：上面那些义务**没有一条由类型或语法保证**，它们
只写在注释和人的记忆里。C 里同样的写法反而不悬，因为
`pthread_cond_wait(&cv, &mutex)` 把"这个谓词由谁保护"写进了**参数**；
JS 省掉了锁（无抢占的便宜），代价是**连那个句法锚点一起没了**，只剩
一条约定：`pulling === null` 表示空闲。

现况逐条：

| 义务                 | 现状 | 靠什么                                |
| -------------------- | ---- | ------------------------------------- |
| 每轮必须推进         | 守住 | 单飞 `pull()` 是唯一出路              |
| 判据单向不可回退     | 守住 | 水位是代理自持字段，策略够不着        |
| 终局必须响铃         | 半守 | done 进判据、源错抛穿；cancel 未接    |
| 槽必须回位           | 守住 | `finally` 承重（纪律非结构）          |
| 循环体必须廉价       | 守住 | 也是纪律（等待者会放大）              |
| 体内不得另有 `await` | 守住 | 槽挡住重入（`pull → enqueue → pull`） |

"判据单向"这一条是后来从"未守"翻成"守住"的：水位一旦搬到代理自持
的字段上，`$I.DROP` 再也拆不掉它。

另有一处偶然性值得记下：`finally` 的回位先于等待者的重判，这里成立，
但它依赖微任务的挂接顺序。真正让这个偶然无害的恰恰是那圈 `while`
——顺序错了只会多拉一次，而多拉一次不是错误，只是多花一次往返。
在这个构造里，`while` 同时也是容错垫。

### 危险不能消除，只能转移或显式化

1. **让非法状态不可表示**：把判据抽成一个 `ready(target)` 纯谓词，循环
   只做"读谓词 + 调推进"。义务集中到一处，不再散落在两半之间。
2. **让推进结构性成立**：只允许经单飞 `pull()` 落地；"这一轮不落地"
   必须写成 `await` 一个结算点，不能是 `return`。
3. **让义务可执行**：开发期探针断言"每轮之后判据必变化"。这是把活性
   从散文里抠出来变成会红的东西——也是唯一能让未来改动不静默失效的
   手段。

至于换做法（等待者登记表），它不是风格偏好：义务不会消失，只会从
等待者侧搬到落地侧（落地时必须扫表，终局时也必须扫表）。判断标准
因此很具体——**当判据变成多源（内存 / 介质 / 终局）时就该搬**；现在只有
一条判据、一个槽，水位自持之后它还是单源的，三行更划算。

## 实测：循环体执行次数

| 场景                    | 真实拉取 | 循环体执行                |
| ----------------------- | -------- | ------------------------- |
| 3 条 fork 同时读前沿    | 1        | 3（3 个等待者各加入一次） |
| 同一条 fork 顺序读 6 次 | 6        | 6（每轮起点水位 = 0…5）   |
| `ensure(5)`、水位为 0   | 6        | 6（目标在前方）           |
| 落后 4 块的新 fork      | 0        | 0（首次判据即不成立）     |
| 拉取不再推进落地        | ∞        | 1001+（空转）             |

判据 `pulledChunkCount` 是代理自持的单调量（只增；策略够不着它），而
读者的 `target` 又必然 ≤ 它——读者要下标 k，说明它此前已消费 k 块，
每一块都经过交接。于是 `target ≤ pulledChunkCount` 恒成立、判据只可能
取等，循环体**至多执行一次**：落后于前沿的读者 0 轮，站在前沿的读者
（含每条新 fork）1 轮。这一点与相位无关：降级把 stash `$I.DROP` 空也
不影响水位。

由这个不变式可推得：今天把 `while` 写成 `if` 也会通过同样这些探测。
之所以仍然写 `while`，是因为"这一轮不落地"（背压）还在 TODO 里排队
——那时 `if` 会退化成静默短读。降级那条 TODO 已经不在了：水位跨相位，
不需要"换成介质进度"。

降级那条也验过：`stashByteLimit` 触发降级、真 `$I.DROP`（`length` 从此
恒为 0、介质接管）之后，读者照常读到 `0…5` 与 EOS，真实拉取 7 次、
无空转——判据不依赖那个被 DROP 的容器。
